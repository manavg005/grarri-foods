/// A class that holds configuration settings for the Flutter application.
class MyAppSettings {
  /// Private constructor to prevent direct instantiation of the class.
  MyAppSettings._privateConstructor();

  /// Indicates whether the application is in production mode.
  static const bool isProduction = true;

  /// Indicates whether the application is in QA (Quality Assurance) mode.
  static const bool isQA = false;
}
